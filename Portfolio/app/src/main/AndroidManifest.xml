<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nitishkumar.portfolio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".featurepage"></activity>
        <activity android:name=".eduqua"></activity>
        <activity android:name=".skill"></activity>
        <activity android:name=".profile"></activity>
        <activity android:name=".CourseCompleted"></activity>
        <activity android:name=".Projects"></activity>
        <activity android:name=".PORs"></activity>
    </application>

</manifest>
